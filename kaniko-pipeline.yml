trigger:
  branches:
    include:
    - main

pool:
  name: tzl-wl
  demands:
    - agent.name -equals tzl-wl-ado-596bc54955-sjsxs

steps:
- checkout: self

- task: Kubernetes@1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'tzlpoc'
    namespace: 'default'
    command: 'get'
    arguments: 'pods'
    secretType: 'dockerRegistry'
    containerRegistryType: 'Azure Container Registry'


